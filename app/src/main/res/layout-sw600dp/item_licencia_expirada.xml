<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@android:drawable/ic_menu_report_image"
        app:layout_constraintVertical_weight="70"
        android:id="@+id/imageViewLicenciaExpirada"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/textViewLicenciaExpirada"/>

    <TextView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="Test text"
        android:textSize="20sp"
        android:gravity="center"
        app:layout_constraintVertical_weight="30"
        android:id="@+id/textViewLicenciaExpirada"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/imageViewLicenciaExpirada"/>
</androidx.constraintlayout.widget.ConstraintLayout>